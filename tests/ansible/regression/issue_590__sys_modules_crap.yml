
- hosts: test-targets
  tasks:
  - meta: end_play
    when: ansible_version.full is version_compare('2.8', '<', strict=true)

  - custom_python_uses_distro:
    register: out

  - assert:
      that:
      - "'id' in out.info"
